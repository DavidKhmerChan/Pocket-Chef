<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_add_instructions"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="davidchan.pocketchef.AddInstructionsActivity">

    <EditText
        android:id="@+id/instructions_text"
        android:hint="Instructions"
        android:gravity="center"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <EditText
        android:id="@+id/duration_text"
        android:hint="Duration (in seconds)"
        android:gravity="center"
        android:layout_below="@id/instructions_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/add_instructions_go"
        android:text="Add Instruction"
        android:layout_below="@+id/duration_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <ListView
        android:id="@+id/instructions_list_preview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/add_instructions_go"
        android:layout_above="@+id/add_instructions_finish">

    </ListView>

    <Button
        android:id="@+id/add_instructions_finish"
        android:text="Finish"
        android:layout_alignParentBottom="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

</RelativeLayout>
